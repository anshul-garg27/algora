# Q: Tell me about a time when you took complete ownership of a project and ensured its success.

> **LP**: Ownership
> **Primary story**: `W8 — DC Inventory Search API`
> **Backup story**: `G7 — Sole Architect for 6 Services`
> **Time budget**: 90–120 seconds spoken

---

## STAR — how to actually tell it

### Situation

In August 2025, the product team came to us with a need. Suppliers like Pepsi and Coca-Cola wanted a way to query Walmart's distribution-centre inventory in bulk — "give me on-hand and in-transit counts for these 50 SKUs at DC 6012". No such API existed. The consumer team was already booked for January integration.

### Task

Build the DC Inventory Search API from spec to production. I was the only backend engineer on it. Eight PRs over five months, end-to-end.

### Action

I started with the spec, not the code. That was deliberate. Wrote 898 lines of OpenAPI in PR #260 — endpoints, request/response schemas, validation rules, error examples for every status code. Shared it with the consumer team day one so they could generate a client SDK and start mocking. They didn't have to wait for my implementation.

The implementation in PR #271 was about 3,000 lines. The shape that mattered was a 3-stage pipeline. Stage 1 converts WmItemNumbers to GTINs through UberKey. Stage 2 validates each GTIN against supplier authorization. Stage 3 fetches actual inventory from the Enterprise Inventory API. Each stage tracks its own errors. So a 100-item bulk request might come back as 80 successes and 20 errors from three different stages — and the consumer knows exactly what failed and why.

For multi-country support I used a factory pattern. `USConfig`, `CAConfig`, `MXConfig` all implement `SiteConfigProvider` and Spring auto-discovers them into a `Map<String, SiteConfigProvider>`. Adding Mexico was one class plus an `@Component("MX")`. Zero changes to the factory or controller.

About a month after launch, I realised the validation logic was duplicated between this API and the Search Items API. PR #322 — about 1,900 lines — centralised it into a `RequestProcessor` pipeline. Each stage takes current state, runs a function on valid items, tags failures with an error source.

Then I caught a bug in container tests that unit tests had missed. Some GTINs mapped to multiple WmItemNumbers, and my reverse-mapping picked one non-deterministically. PR #330 added null checks, duplicate logging, and deterministic selection. Then PR #338 — 1,724 lines of container tests with Docker, WireMock, and real PostgreSQL test data — so this category of bug couldn't slip again.

### Result

Live across US, Canada, and Mexico. Bulk requests up to 100 items per call. About 8,000 lines from spec to container tests across eight PRs. The design-first approach cut consumer integration time by roughly 30% — they started three weeks earlier than they would have with code-first. And the next country we'll add (Brazil) is one class.

The lesson for me: write container tests from the start, not as an afterthought. PR #338 caught the GTIN reverse-mapping bug; unit tests with mocks never could have.

---

## Technical depth — if they probe

- **Design-first OpenAPI**: Spec is the source of truth. The controller `implements InventorySearchDistributionCenterStatusApi`, an interface generated by `openapi-generator-maven-plugin`. If the spec changes, the controller stops compiling until it's updated. Strongest form of spec-implementation contract.
- **Always HTTP 200 with partial success**: A bulk request where 80 items succeed and 20 fail returns 200, not 207 or 400. Each item has `dataRetrievalStatus = SUCCESS | ERROR` with a reason. If we returned 400 the consumer's error handler might discard the 80 good results.
- **GTIN-to-WmItemNumber reverse conversion**: Internal stages work with GTINs because that's what supplier mapping uses. But errors return WmItemNumbers because that's what the consumer sent. Small UX choice, real impact on debugging time for the supplier.
- **`ConcurrentHashMap` for shared pipeline state**: Map is populated in Stage 1 and read in Stages 2 and 3. Even though current execution is mostly sequential, ConcurrentHashMap is defensive — if someone parallelises bulk processing later, no race condition. Also disallows null keys, prevents subtle NPE.
- **Java records for EI response models**: `DCInventoryPayload`, `EIDCInventoryResponse` — immutable data carriers. Auto-generated equals/hashCode/toString. `@JsonIgnoreProperties(ignoreUnknown = true)` so upstream EI API additions don't break us.

---

## Likely follow-ups

**Q: Why design-first and not code-first?**
> In code-first, consumers wait for your implementation before they can start. In design-first, the spec exists from day one, they generate a client SDK and mock against it, and integration happens in parallel. About 30% less calendar time for the same delivery.

**Q: How did you decide on a 3-stage pipeline?**
> Bulk requests can fail at different stages for different reasons. An invalid identifier (Stage 1), unauthorised supplier access (Stage 2), no inventory data (Stage 3). The pipeline pattern with `RequestProcessor` tracks errors by source so the consumer sees exactly where each item failed.

**Q: What did you get wrong?**
> Two things. Container tests should have been in PR #271, not PR #338 two months later. And I should have built `RequestProcessor` in the first iteration — when I built the second API and saw the same validation logic, that was the warning sign I should have caught earlier.

**Q: How did you handle multi-country?**
> Factory pattern with Spring DI. `SiteConfigFactory` holds a `Map<String, SiteConfigProvider>`. Each country is `@Component("US")`, `@Component("CA")`, `@Component("MX")`. Spring auto-discovers all of them. Adding Brazil = new `BRConfig` class. Zero changes to the factory.

**Q: Did you do code reviews yourself?**
> No — senior engineers on the team reviewed each PR. But I owned every decision and every defect. Eight PRs is a lot of design context for a reviewer to hold; my job was making sure each PR was self-contained and clear.

---

## What NOT to say

- Don't claim you wrote the spec alone — review with consumers shaped about 20% of it.
- Don't say "I built it from scratch" — the supplier authorization framework existed; I integrated with it.
- Don't claim 100% test coverage — container tests came late (PR #338), and the GTIN bug shipped to prod before they caught it.
- Don't pitch CompletableFuture for DC Inventory parallelism — the EI backend aggregates across DCs internally; CompletableFuture lives in the Kafka publisher (W1), not here.

---

## Backup story (if asked for another)

At Good Creator Co. I was an SE-I owning six backend services solo — Beat, Event-gRPC, Stir, Coffee, the API gateway, and a fake-follower ML pipeline. About 60K lines of Go and Python over 15 months, processing 10M data points a day for brand clients. No one assigned that scope; I picked it up because the team was four engineers and someone had to.
